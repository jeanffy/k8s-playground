apiVersion: v1
kind: Service
metadata:
  name: nodulfront-nodeport
  labels:
    app.kubernetes.io/name: nodulapp
    app.kubernetes.io/component: nodulfront
spec:
  type: NodePort
  ports:
    - name: http
      port: 3334
      targetPort: 7474
      nodePort: 32222
  selector:
    app.kubernetes.io/component: nodulfront
